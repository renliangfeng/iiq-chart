apiVersion: v1
kind: Service
metadata:
  name: {{ include "iiq-helm.fullname" . }}
  labels:
    {{- include "iiq-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "iiq-helm.selectorLabels" . | nindent 4 }}
