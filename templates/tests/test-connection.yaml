apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iiq-helm.fullname" . }}-test-connection"
  labels:
    {{- include "iiq-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iiq-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
